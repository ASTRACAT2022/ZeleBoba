{% extends 'base.html.twig' %}{% block title %}Операции{% endblock %}{% block content %}<header><div><p class="eyebrow">ОПЕРАЦИОННЫЙ ЦЕНТР</p><h1>Операции.</h1><p class="muted">Полная цепочка платежей, подписок, очередей и выдачи VPN.</p></div></header>{% include '_admin-nav.html.twig' %}
<section class="panel detail"><form method="get" class="row-form"><label>Поиск<input name="q" value="{{ q }}" placeholder="Платёж, заказ, подписка, пользователь, Telegram, связь…"></label><label>Операция<select name="type"><option value="">Все</option>{% for value,label in {'payment.apply':'Зачисление платежа','subscription.renew':'Продление подписки','provisioning.retry':'Повтор выдачи VPN','reconciliation.subscription':'Сверка подписки','wallet.debit':'Списание баланса'} %}<option value="{{ value }}" {{ type == value ? 'selected' : '' }}>{{ label }}</option>{% endfor %}</select></label><label>Статус<select name="status"><option value="">Все</option>{% for value,label in {'success':'Успешно','processing':'В процессе','warning':'Требует внимания','failed':'Ошибка'} %}<option value="{{ value }}" {{ status == value ? 'selected' : '' }}>{{ label }}</option>{% endfor %}</select></label><label>Период<select name="period">{% for value,label in {'today':'Сегодня','24h':'24 часа','7d':'7 дней','30d':'30 дней'} %}<option value="{{ value }}" {{ period == value ? 'selected' : '' }}>{{ label }}</option>{% endfor %}</select></label><button class="button small">Найти</button></form></section>
<section class="panel detail"><h2>Операции</h2>{% if operations %}<table><thead><tr><th>Операция</th><th>Статус</th><th>Пользователь</th><th>Связанные объекты</th><th>Время</th></tr></thead><tbody>{% for o in operations %}<tr><td><a href="/admin/operations/{{ o.id }}">{{ o.type }}</a><br><small>{{ o.correlation_id }}</small></td><td><span class="badge {{ o.status == 'success' ? 'fulfilled' : (o.status == 'failed' ? 'canceled' : 'pending') }}">{{ o.status }}</span></td><td>{{ o.email ?? (o.telegram_id ? 'TG ' ~ o.telegram_id : o.user_id|slice(0,8)) }}</td><td><small>{{ o.payment_id ?? o.order_id ?? o.subscription_id ?? '—' }}</small></td><td>{{ o.started_at|date('d.m.Y H:i:s') }}</td></tr>{% endfor %}</tbody></table>{% else %}<p class="muted">Операций по этому запросу нет.</p>{% endif %}</section>{% endblock %}
